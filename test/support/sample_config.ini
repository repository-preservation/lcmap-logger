[LCMAP Client]

username = alice
password = secret
version = 0.5
endpoint = http://127.0.0.1:1077
content-type = json
log-level = error
logging-namespaces = lcmap-client

[LCMAP Server]

auth-backend = usgs
auth-usgs-endpoint = http://127.0.0.1:8888/api
db-hosts = 127.0.0.1
db-credentials-username = alice
db-credentials-password = secret
